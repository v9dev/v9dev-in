# Resume notes - 2026-06-05 (v9 Deck game)

Stopping for the day. Pick up here tomorrow.

## Branch: `feat/v9-deck` (NOT merged, ready to merge pending your play-test)

The deck is now a **terminal-driven game** (was a showcase first - the
user rejected that; see memory `build-games-not-showcases`). ~29 commits
off `main`. Final whole-branch review verdict: **ready to merge - yes**.

Spec: `docs/superpowers/specs/2026-06-05-v9-deck-game-design.md`
Plan: `docs/superpowers/plans/2026-06-05-v9-deck-game.md`
(Earlier showcase spec/plan: `...-v9-deck-design.md` / `...-v9-deck.md`.)

### What it is
`/deck` opens to a menu. `ls` lists 4 levels (`stalwart-mail` easy,
`cockpit`, `webstack` medium, `ci` hard). `play <id>` starts UNWIRED at
0%. Wire by command (`connect a b` / `disconnect a b`) or by drag; nodes
light up as they come online; HUD tracks % / connections / moves / timer
/ hints. `status`, `hint`, then `boot`/`fireup` wins only when correct
(SYSTEM ONLINE + time/moves/hints), `next` advances, `menu` returns.
Skills chart and the fully-wired showcase view are gone.

### Verified (objective)
- `pnpm test` deck+scenario suites: 69 pass (state/board/commands/
  architectures). Full suite: only the pre-existing unrelated
  `functions/_lib/validate.test.ts` fails.
- `pnpm run typecheck`: 0 errors. Deck files biome-clean.
- `/deck` serves HTTP 200 in dev, no runtime errors.
- All 4 levels are winnable (a test asserts every required wire passes
  `canConnect`).

### TODO tomorrow (in order)
1. **Play-test** `pnpm dev` -> http://localhost:4321/deck:
   `ls` -> `play stalwart-mail` -> wire by command AND drag (% rises,
   nodes light, drag echoes `connect a b`) -> `status` / `hint` ->
   `boot` incomplete (unreachable report) -> finish -> `boot` win ->
   `next` (cockpit). Check reduced-motion, keyboard wiring, touch; no
   skills chart anywhere; homepage teaser before Notes links to /deck.
2. **Optional polish (user to decide):** on a winning `boot`, the
   WinPanel replaces the Diagram instantly, so the on-diagram boot
   "fire-up" pulse is not seen (terminal still prints `boot: ... up`).
   Fix = play the boot animation first, then show WinPanel. Other known
   minors: `data-cursor="drag"` is on the canvas always (cosmetic);
   tap-to-disconnect only works on single-inbound nodes (terminal
   `disconnect a b` always works).
3. **Finish:** `superpowers:finishing-a-development-branch` -> merge
   `feat/v9-deck` to `main` locally (a couple of duplicate-message
   fix-loop commits exist; squash on merge if a clean history is wanted).
   Then USER pushes `main` to origin with their own creds (the
   `iamjpsingh` identity here is read-only on `v9dev/v9dev-in`). `main`
   is already ahead of `origin/main`.

## Conventions
pnpm only; review via dev HMR (no `pnpm build`); commits authored
`v9dev <99959044+v9dev@users.noreply.github.com>`, no AI trailer; copy
uses hyphens, never em-dashes (middot allowed).

## Other open branch (unrelated, untouched today)
`feat/constellation-interactive-polish` - still push-pending + needs
manual visual QA (see `RESUME-2026-05-15.md`). Do NOT merge it (predates
SEO; would delete the SEO assets - cherry-pick only if ever needed).

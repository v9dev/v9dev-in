# Resume notes — 2026-05-15

Two independent workstreams in progress. Working tree clean; all work committed.

## Branch 1: `feat/constellation-interactive-polish` (DONE, push-pending)

7 commits off `main`. Feature complete: brand-color ignite, magnetic 3D
hover, hovered-node path highlight, parallax depth, reduced-motion gated.
Two-stage reviewed per task + final whole-branch review — **approved**.

- Automated checks green for the scoped work. The one `pnpm test`
  failure (`functions/_lib/validate.test.ts`) is pre-existing,
  unrelated, untouched by this branch (HANDOFF.md item 1).
- **Not pushed**: the GitHub identity available here (`iamjpsingh`) is
  read-only on `v9dev/v9dev-in`. User will push with their own creds.
- Ready-to-run push + PR command (title/body) is in the prior session
  output. **Pending: manual visual QA via `pnpm dev`** (ignite, hover,
  parallax, reduced-motion, touch) before merge.

## Branch 2: `feat/seo-assets-polish` (IN PROGRESS — resume here)

Off `main`. Spec + plan committed
(`docs/superpowers/specs/2026-05-15-seo-assets-polish-design.md`,
`docs/superpowers/plans/2026-05-15-seo-assets-polish.md`).

Plan = 5 tasks. **Task 1 committed (`748c1e4`)** but has OPEN code-review
findings to fix BEFORE marking it complete:

1. **Important** — `scripts/seo-assets.config.mjs` `OG.role` uses ASCII
   `-` but `src/lib/seo.ts` `SITE.title` uses middle dot `·` between
   "Forward Deployed Engineer" and "Cloud + Full-stack". Change
   `OG.role` to `'Forward Deployed Engineer · Cloud + Full-stack'` so
   the card matches the page title. (Middot is allowed; only em-dashes
   are banned.)
2. **Minor (recommended)** — add `export const SOURCE_LOGO =
   'assets/brand/v9-logo.jpg';` to the config so Task 2's generator
   imports the source path instead of hardcoding it. If done, update
   Task 2 in the plan to consume `SOURCE_LOGO`.
3. **Minor** — fix the misleading "mirrors the site tokens" comment:
   `COLORS.muted` (`rgba(245,245,240,0.62)`) is a deliberate
   canvas-compositing variant, not the CSS `--color-muted` grey.

`OG.tagline` confirmed exact-match with `SITE.description` (no drift).

### Next actions tomorrow (in order)

1. Fix the 3 findings above in `scripts/seo-assets.config.mjs` (commit:
   `fix(seo): align OG role separator, add SOURCE_LOGO, clarify comment`).
   Re-run the Task 1 code-quality review, then mark Task 1 complete.
2. Resume subagent-driven execution: Plan **Task 2** (favicon/icon
   generator), **Task 3** (OG png+webp), **Task 4** (wire
   SeoHead/manifest), **Task 5** (full verification). Two-stage review
   each.
3. Verified runtime facts already baked into the plan: use
   `@playwright/test` (NOT `playwright`); Chromium installed; `sips` has
   no WebP (WebP via Chromium canvas); `scripts/` is biome-ignored.
4. Final whole-branch review → `finishing-a-development-branch`. Same
   push caveat as branch 1 (user pushes).

## Conventions (both branches)

pnpm only; no `pnpm build` to iterate; commits authored
`v9dev <99959044+v9dev@users.noreply.github.com>` with NO AI co-author
trailer; copy uses hyphens, never em-dashes (middot `·` allowed).
